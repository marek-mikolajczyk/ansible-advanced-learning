- git pull !!!

-  map filter has 2 usages - either get attribute, or use another filter with it's parameters
* {{ public_key_lists | map(attribute='public_keys') | flatten }} 
- default, True if you want to change false/null var. if you want to leave it empty/null - use default, False
- default(omit) also has True/False

- subelements - LNL-S has a tip with old syntax with_subelements

- LOL-F, LNL-S, LDL-M

- together lists with union, dictionaries with combine

- move vars from default/main.yml to group_vars or host_vars

- estimate if you need list. if yes, add | list

- query('pipe') vs query('lines') - is for command lines. think about it

- errors in lookup might be many, so we use errorS . we can be STRICT, we can IGNORE, or we just WARN

- batches like max_fail_percentage: 30% or even max_fail_percentage: 0 very very strics


- Tower organization - TPI=O
- for organizoation object permissions, only users not teams

- import inventories via awx-manage - inventory must exist